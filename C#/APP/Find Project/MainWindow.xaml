<Window x:Class="Find_Project.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Find_Project"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        
        mc:Ignorable="d"
        Title="Find Project" Height="600" Width="1080" WindowStartupLocation="CenterScreen" WindowStyle="none" Foreground="White">
    <Window.Template>
        <ControlTemplate TargetType="{x:Type Window}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Style="{StaticResource HeaderStyle}" Padding="10">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="_" Width="30" Style="{StaticResource ButtonStyle}" Click="MinimizeButton_Click" Margin="0,0,10,0"/>
                        <Button Content="X" Width="30" Style="{StaticResource ButtonStyle}" Click="CloseButton_Click"/>
                    </StackPanel>
                </Border>
                <Border Grid.Row="1" Background="{TemplateBinding Background}">
                    <AdornerDecorator>
                        <ContentPresenter/>
                    </AdornerDecorator>
                </Border>
            </Grid>
        </ControlTemplate>
    </Window.Template>
    <Grid>
        <TabControl>
            <TabItem Header="Search" Style="{StaticResource TabItemStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="10">
                        <TextBox x:Name="searchTextBox" Width="200" Margin="0,0,10,0" Style="{StaticResource TextBoxStyle}" MaxLength="50"/>
                        <Button x:Name="searchButton" Content="Search" Style="{StaticResource ButtonStyle}"/>
                    </StackPanel>
                    <ListBox x:Name="resultsListBox" Grid.Row="1" Margin="10">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Style="{StaticResource TextStyle}"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </TabItem>
            <TabItem Header="Settings" Style="{StaticResource TabItemStyle}">
                <StackPanel Margin="10">
                    <TextBlock Text="Customization Options:" FontWeight="Bold" Style="{StaticResource TextStyle}"/>
                    <ComboBox x:Name="ThemeComboBox" SelectionChanged="ThemeComboBox_SelectionChanged">
                        <ComboBoxItem>LightTheme</ComboBoxItem>
                        <ComboBoxItem>DarkTheme</ComboBoxItem>
                    </ComboBox>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Label Content="Enter a number:" Style="{StaticResource TextStyle}"/>
                        <TextBox x:Name="inputTextBox" Width="50" Style="{StaticResource TextBoxStyle}"/>
                    </StackPanel>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>

</Window>
